name: 'Proselint Action'
description: 'Run Proselyt to lint prose files'
author: 'Your Name'

inputs:
  path:
    description: 'Path to the directory or file to lint'
    required: true
    default: './prose_files'

runs:
  using: 'docker'
  image: 'Dockerfile'

